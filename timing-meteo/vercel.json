{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/timing-meteo/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}